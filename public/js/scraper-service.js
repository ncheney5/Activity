/**
 * BYU–Idaho student activities scraper (stub).
 * When Cloud Functions billing is available, replace with a callable/backend job
 * that writes into the `activities` collection with `source: "byui-scrape"`.
 */

export async function fetchByuiActivitiesPreview() {
  return [];
}
